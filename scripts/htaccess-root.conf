# This file is intentionally empty.
#
# fasl-work.com is the PRIMARY domain on the cPanel account,
# so it serves directly from public_html/ without rewrite rules.
#
# The htaccess rewrite strategy was needed when the site lived in
# a subdirectory, but since fasl-work.com = primary domain,
# cPanel handles the routing via Apache VirtualHost.

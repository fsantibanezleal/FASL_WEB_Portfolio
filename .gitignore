# build output
# dist/ is intentionally committed — VPS has no Node, so it pulls the
# pre-built artifact directly. See scripts/local.sh preview.
.output/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables
.env
.env.production

# macOS-specific files
.DS_Store

pnpm-lock.yaml

.astro